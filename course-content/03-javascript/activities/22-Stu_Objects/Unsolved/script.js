// WRITE YOUR CODE BELOW
const customerDrink = {
  drinkName: "coffee",
  numberOfSugars: 2,
  orderReady: false,
};

if (customerDrink.orderReady === true) {
  console.log(`"Ready for pick-up"`);
} else {
  console.log(`"Still in order queue"`);
}
// avoid return when logging to console
// objects use dot notation
// lookup syntax when unsure
// quotes work in a string literal with backticks
